/**
 * Generates a unique, sortable ID using ULID
 * @param prefix - Optional prefix to prepend to the ID
 * @returns A unique ID string, optionally prefixed
 */
export declare function id(prefix?: string): string
